/**
 * User-facing app version, shown in Settings.
 *
 * Mirrors the mobile Settings "Version" row so support can ask the same
 * question on either platform and get a comparable answer. Kept in step with
 * `apps/web/package.json` version.
 */
export const APP_VERSION = '1.0.0';
